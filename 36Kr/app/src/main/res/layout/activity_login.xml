<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3"
        android:background="@color/backgroundOfFindAndMy"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/login_quit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/normal_margin"
            android:src="@mipmap/news_krtv_close" />

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_margin="@dimen/login_logo_margin"
            android:layout_weight="1"
            android:src="@mipmap/login_icon_logo" />

        <android.support.design.widget.TabLayout
            android:id="@+id/login_tab_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:tabIndicatorColor="@null"
            app:tabSelectedTextColor="@color/login_tab_text_after"
            app:tabTextColor="@color/login_tab_text_before"/>

    </LinearLayout>

    <android.support.v4.view.ViewPager

        android:id="@+id/login_vp"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="5"
        android:orientation="vertical"></android.support.v4.view.ViewPager>

</LinearLayout>